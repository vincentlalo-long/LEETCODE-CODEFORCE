#include <iostream>
#include <vector>
#include <map>
#include <string>
#include <ctime>
#include <algorithm>

 
using namespace std ;
typedef long long int ll ;
 
#define MM 1E-9
#define INF 1E18
 
void solve() {   
    int K, X ;
    cin >> K >> X ;

    cout << (K * X) + 1 ;
 
    return ;
}
 
int main()
{ 
    ios_base::sync_with_stdio(false) ; 
    cin.tie(NULL) ;
    int t ;
    cin >> t ;
    while (t--) {
        solve() ;
        cout << "\n" ;
    }
    return 0 ; 
} 